apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: app-migration-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: app-migration
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 15s